// Contract: Lemonade Stand

// the point is to create commented blocks of code, with their purpose

// Variable: owner

// Variable: skuCount

// Mapping: assign item to sku

// State: ForSale, Sold

// Struct: Item { name, sku, price, state, seller, buyer }

// Modifier: 'onlyOwner' can call the function (for functions only the owner can use)

// Modifier: 'verifyCaller' can call the function to list caller address

// Modifier: 'paidEnough' has paid enough to purchase lemonade on sale

// Modifier: 'forSale' check if lemonade is for sale

// Modifier: 'sold' to check if the lemonade has been sold

// Constructor Function: Set 'owner' to the address that instantiated the contract

// Function: 'addItem' allows owner to add new items to the inventory

// Function: 'buyItem' allows user to buy item that is for sale.

// Function: 'fetchItem' that fetches the data of any of the items on the smart contract

//